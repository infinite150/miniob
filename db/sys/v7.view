{
	"columns" : 
	[
		"num",
		"data"
	],
	"select_sql" : "select count(t1.id) as num, sum(t1.age)+sum(t2.age) as data from create_view_t1 t1, create_view_t1 t2 where t1.id=t2.id",
	"view_name" : "v7"
}